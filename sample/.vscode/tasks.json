{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Babel",
            "command": "./node_modules/.bin/babel ./src -w --out-dir bbl --extensions \".ts,.tsx\"",
            "type": "shell",
            "group": "build"
        }
    ]
}